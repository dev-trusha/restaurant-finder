<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Finder</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
    <!-- load auth helpers early so view inline scripts can call storeToken/authFetch -->
    <script src="/js/auth.js"></script>
</head>
<script>
    // Clear client-side storage when logging out
    document.addEventListener('DOMContentLoaded', function() {
        // When logout links are clicked, clear localStorage
        const logoutLinks = document.querySelectorAll('#logoutBtn, #footerLogout');
        
        logoutLinks.forEach(link => {
            link.addEventListener('click', function(e) {
                // Clear localStorage and sessionStorage
                localStorage.removeItem('authToken');
                localStorage.removeItem('user');
                sessionStorage.removeItem('authToken');
                sessionStorage.removeItem('user');
            });
        });
    });
</script>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">
                <span class="fs-4">🍽️</span> Restaurant Finder
            </a>
            
            <div class="navbar-nav ms-auto">
                {{#if user}}
                    <!-- Show when user is logged in -->
                    <div class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            👤 {{user.username}}
                        </a>
                        <ul class="dropdown-menu">
                            <li><span class="dropdown-item-text">Role: {{user.role}}</span></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/auth/logout" id="logoutBtn">🚪 Logout</a></li>
                        </ul>
                    </div>
                {{else}}
                    <!-- Show when user is NOT logged in -->
                    <a class="nav-link" href="/auth/login">🔑 Login</a>
                    <a class="nav-link" href="/auth/register">📝 Register</a>
                {{/if}}
                
                <a class="nav-link" href="/">
                    🏠 Home
                </a>
                <a class="nav-link" href="/restaurants/search">
                    🔍 Search
                </a>
                <a class="nav-link" href="/analytics">
                    📊 Analytics
                </a>
                {{#if user}}
                <a class="nav-link" href="/restaurants/create">
                    ➕ Add Restaurant
                </a>
                {{/if}}
            </div>
        </div>
    </nav>
    <main class="container mt-4">
        {{{body}}}
    </main>

        <footer class="bg-dark text-white mt-5 py-4">
            <div class="container text-center">
                <p>&copy; 2024 Restaurant Finder. ITE5315 Project.</p>
                <p class="small">
                    {{#if user}}
                        Logged in as: <strong>{{user.email}}</strong> | 
                        <a href="/auth/logout" id="footerLogout" class="text-white-50">Logout</a>
                    {{else}}
                        <a href="/auth/login" class="text-white-50">Login</a> | 
                        <a href="/auth/register" class="text-white-50">Register</a>
                    {{/if}}
                </p>
            </div>
        </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // Logout functionality - SIMPLE AND RELIABLE
    document.addEventListener('DOMContentLoaded', function() {
        // Handle logout button clicks
        document.addEventListener('click', function(e) {
            if (e.target.id === 'logoutBtn' || e.target.id === 'footerLogout' || 
                e.target.closest('#logoutBtn') || e.target.closest('#footerLogout')) {
                e.preventDefault();
                
                // Clear all client-side storage
                localStorage.clear();
                sessionStorage.clear();
                
                // Call server logout
                window.location.href = '/auth/logout';
            }
        });
        
        // Add click handler to any element with class 'logout-link'
        document.querySelectorAll('.logout-link').forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                localStorage.clear();
                sessionStorage.clear();
                window.location.href = '/auth/logout';
            });
        });
    });
</script>
</body>
</html>