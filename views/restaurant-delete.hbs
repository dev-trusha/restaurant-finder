<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h3 class="mb-0">🗑️ Delete Restaurant</h3>
            </div>
            <div class="card-body text-center">
                <div class="display-1 text-danger mb-4">⚠️</div>
                <h4 class="mb-3">Are you sure you want to delete?</h4>
                
                <div class="card mb-4">
                    <div class="card-body">
                        <h5 class="card-title">{{restaurant.name}}</h5>
                        <p class="card-text mb-1">
                            <strong>📍</strong> {{restaurant.address.city}}, {{restaurant.address.country}}
                        </p>
                        <p class="card-text mb-1">
                            <strong>⭐</strong> {{restaurant.rating}}/5 • <strong>💰</strong> {{restaurant.priceRange}}
                        </p>
                        <p class="card-text">
                            <strong>🍽️</strong> {{join restaurant.cuisines ", "}}
                        </p>
                    </div>
                </div>

                <p class="text-danger mb-4">
                    <strong>Warning:</strong> This action cannot be undone. All restaurant data will be permanently deleted.
                </p>

                <form method="POST" action="/restaurants/{{restaurant._id}}/delete" class="d-inline">
                    <button type="submit" class="btn btn-danger btn-lg px-4">
                        🗑️ Yes, Delete Restaurant
                    </button>
                </form>
                
                <a href="/restaurants/{{restaurant._id}}" class="btn btn-outline-secondary btn-lg px-4 ms-2">
                    ✋ Cancel
                </a>
            </div>
        </div>
    </div>
</div>